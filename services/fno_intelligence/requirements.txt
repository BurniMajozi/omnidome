fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0
# ── Passed-homes import (CSV/XLSX parsing) + file upload ──────────────
pandas>=2.0.0
openpyxl>=3.1.0
# aiofiles was already used by the KML upload path (routes.py) without being
# pinned anywhere in this file -- that endpoint would ModuleNotFoundError at
# runtime. Pinning it here fixes that pre-existing gap and covers the new
# passed-homes upload path too.
aiofiles>=23.0.0
# ── Shared services/common dependencies ───────────────────────────────
# fno_intelligence imports services.common (auth, db, firecrawl), which
# requires the following at runtime. Keep in sync with services/common/requirements.txt.
python-dotenv==1.0.1
python-multipart==0.0.9
cryptography==42.0.8
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
PyJWT==2.9.0
bcrypt==4.2.0
