// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../core/resource.mjs";
import * as RemindersAPI from "./reminders.mjs";
import { Reminders } from "./reminders.mjs";
import { Cursor } from "../../core/pagination.mjs";
/**
 * Chats operations
 */
export class Chats extends APIResource {
    constructor() {
        super(...arguments);
        this.reminders = new RemindersAPI.Reminders(this._client);
    }
    /**
     * Create a single or group chat on a specific account using participant IDs and
     * optional title.
     *
     * @example
     * ```ts
     * const chat = await client.chats.create({
     *   accountID:
     *     'local-whatsapp_ba_EvYDBBsZbRQAy3UOSWqG0LuTVkc',
     *   participantIDs: ['string'],
     *   type: 'single',
     * });
     * ```
     */
    create(body, options) {
        return this._client.post('/v0/create-chat', { body, ...options });
    }
    /**
     * Retrieve chat details including metadata, participants, and latest message
     *
     * @example
     * ```ts
     * const chat = await client.chats.retrieve({
     *   chatID: '!NCdzlIaMjZUmvmvyHU:beeper.com',
     * });
     * ```
     */
    retrieve(query, options) {
        return this._client.get('/v0/get-chat', { query, ...options });
    }
    /**
     * Archive or unarchive a chat. Set archived=true to move to archive,
     * archived=false to move back to inbox
     *
     * @example
     * ```ts
     * const baseResponse = await client.chats.archive({
     *   chatID: '!NCdzlIaMjZUmvmvyHU:beeper.com',
     * });
     * ```
     */
    archive(body, options) {
        return this._client.post('/v0/archive-chat', { body, ...options });
    }
    /**
     * Search chats by title/network or participants using Beeper Desktop's renderer
     * algorithm.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const chat of client.chats.search()) {
     *   // ...
     * }
     * ```
     */
    search(query = {}, options) {
        return this._client.getAPIList('/v0/search-chats', (Cursor), { query, ...options });
    }
}
Chats.Reminders = Reminders;
//# sourceMappingURL=chats.mjs.map