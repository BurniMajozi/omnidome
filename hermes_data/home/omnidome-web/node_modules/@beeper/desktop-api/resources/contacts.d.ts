import { APIResource } from "../core/resource.js";
import * as Shared from "./shared.js";
import { APIPromise } from "../core/api-promise.js";
import { RequestOptions } from "../internal/request-options.js";
/**
 * Contacts operations
 */
export declare class Contacts extends APIResource {
    /**
     * Search users across on a specific account using the network's search API. Only
     * use for creating new chats.
     */
    search(query: ContactSearchParams, options?: RequestOptions): APIPromise<ContactSearchResponse>;
}
export interface ContactSearchResponse {
    items: Array<Shared.User>;
}
export interface ContactSearchParams {
    /**
     * Beeper account ID this resource belongs to.
     */
    accountID: string;
    /**
     * Text to search users by. Network-specific behavior.
     */
    query: string;
}
export declare namespace Contacts {
    export { type ContactSearchResponse as ContactSearchResponse, type ContactSearchParams as ContactSearchParams, };
}
//# sourceMappingURL=contacts.d.ts.map