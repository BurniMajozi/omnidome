# OmniDome Daily Review — 2026-07-19 — apps/portal

## 1. Today's plan
Reviewed **apps/portal** — rotation position **3 of 35** (5 apps + 29 services + 1 integration; previous: field-sales-app).

## 2. What was found / achieved
- **apps/portal is completely empty.** Zero files (hidden included, verified by recursive search). The component described in `README.md:99-101` as "Lightweight static HTML/CSS/JS portal for end-customer self-service" does not exist yet.
- **Dev compose serves the empty dir.** `docker-compose.yaml:497-504` defines a `portal` nginx service mounting `./apps/portal:/usr/share/nginx/html/portal` on port 8080 — hitting `/portal` would 403/404.
- **SECURITY (critical): repo root mounted into nginx.** `docker-compose.yaml` line ~502 mounts `.:/usr/share/nginx/html`, exposing the entire repo — including the real `.env` at repo root, `.git`, and all source — over HTTP :8080. The comment says it's for fetching `brand_guidelines` theme; the fix is to mount only `./brand_guidelines` (read-only) plus `./apps/portal`.
- **Not in production compose.** `docker-compose.production.yml` has no portal app service — only `portal-builder` (`services/portal_builder/Dockerfile`, line ~538). So the static portal is dev-only wiring around a nonexistent app.
- **Duplicate empty placeholder.** A second empty `portal/` directory exists at the repo root (`omnidome\portal`) — likely leftover scaffolding.

## 3. Critical decisions / flags
1. **Is apps/portal deprecated in favor of services/portal_builder?** Production only ships portal-builder. Decide: (a) build the static self-service portal from scratch, or (b) drop it — remove the README section, the dev compose `portal` service, and both empty `portal` dirs. Nothing can proceed on this component until this is decided.
2. **Fix the nginx root mount regardless.** Even if the portal is kept, mounting the repo root into a web server exposes `.env` secrets in any dev environment. Change to scoped, read-only mounts.
3. If (a): no spec exists for the portal's scope (which self-service flows, auth model vs Supabase, tenant theming via brand_guidelines) — needs a human-defined scope before implementation.

## 4. Tomorrow's component
**apps/technician-app** (4 of 35).

---

## Addendum (same day, user-approved fixes applied)
- **Security fix applied:** `docker-compose.yaml` portal service no longer mounts repo root. Now scoped, read-only: `./apps/portal` → `/portal` and `./brand_guidelines` → `/brand_guidelines`. `.env`/`.git` no longer reachable via :8080. Warning comment added.
- **Portal scaffolded:** `apps/portal/{index.html, styles.css, app.js}` — static shell with Usage/Billing/Support/Account cards, runtime theming from `../brand_guidelines/default_theme.json` (graceful fallback), and API wiring gated behind `window.PORTAL_API_BASE` (unset by default; cards show "Not connected" and make no backend calls).
- **Still open:** define real self-service flows + auth (Supabase?) and gateway endpoints (`/usage`, `/billing`, `/support`, `/account` are placeholders); decide production serving (portal absent from docker-compose.production.yml); remove or repurpose the empty root-level `portal/` dir.
