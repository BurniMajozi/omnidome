# Daily Review — 2026-07-24 — apps/web

## Today's plan
Component 5 of 35 in rotation (apps: customer-app, field-sales-app, portal, technician-app, **web**, then 29 services + 1 integration). Reviewed the Next.js web app for stub data, hardcoded config, and production blockers. Read-only inspection.

## What was found / achieved
**Stub / mock data**
- `components/modules/journey-ab-testing.tsx:53` — `mockABTests` hardcoded array is the initial state (line 349: `useState<ABTest[]>(mockABTests)`). A/B testing UI ships with fake tests.
- `components/modules/finance/live-journal-entries.tsx:36` — comment admits it sits "alongside the existing mock Journals & Trial Balance panel". Finance module still contains a mock journals/trial-balance panel.
- `components/modules/finance-module.tsx:~234` — hardcoded task list items ("Finalize bank reconciliation", etc.).
- `app/dashboard/customer-360/[customer_id]/page.tsx:59` — "Tab placeholder panels" comment; Customer 360 tabs are placeholders.
- `components/modules/call-center-module.tsx:214` — "mock bucketing" for hourly call histogram.

**Hardcoded config that should be env-driven**
- `lib/supabase/client.ts:3-4` — falls back to `https://placeholder-url.supabase.co` / `"placeholder-anon-key"` when env vars are missing. Fails silently at runtime instead of failing fast at build/start.
- `scripts/verify_hermes_chat.js:4-8` — real Supabase project URL, publishable anon key, and test credentials (`verify-test@omnidome.local` / `VerifyTest123!`) committed in-repo. Publishable key is low-risk by design, but the test account + password shouldn't live in source.
- ~20 API proxy routes (`app/api/**`, `app/svc/**`) and `next.config.mjs:42-59` default to `http://localhost:<port>` / docker-DNS names. Env-driven with fallbacks — acceptable pattern, but inconsistent: some default to docker hostnames (`http://admin:8013`), others to `localhost`, which will break differently depending on where the process runs (next.config.mjs comments acknowledge this).
- `lib/useChannelSocket.ts:28` — websocket falls back to `ws://localhost:8020`.
- `app/api/ai/chat/route.ts:53` — `HTTP-Referer` header hardcoded to `http://localhost:3000` for OpenRouter.

**Error handling**
- No empty catch blocks found (good).
- `app/api/modules/[module]/route.ts` correctly 500s on missing Supabase env — the client-side `lib/supabase/client.ts` should do the equivalent.

## Critical decisions / flags
1. **Supabase placeholder fallback** — decide: throw on missing `NEXT_PUBLIC_SUPABASE_URL/ANON_KEY` at startup vs. current silent placeholder (auth quietly breaks). Recommend fail-fast.
2. **Mock finance panel** — is the mock Journals & Trial Balance panel meant to be removed now that LiveJournalEntries exists, or kept for demo mode? Needs product decision.
3. **journey-ab-testing** — no backend wiring visible; decide whether A/B testing ships in v1 or gets feature-flagged off.
4. **Service URL fallback convention** — pick one default scheme (docker DNS vs localhost) or require env vars in production builds.
5. **Committed test credentials** in `scripts/verify_hermes_chat.js` — rotate the test account password and move to env.

## Tomorrow's component
`services/admin` (6 of 35).
